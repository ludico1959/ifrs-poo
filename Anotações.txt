/////////////////////////////////////////////////////////////////////////////////
// Cap02 - Objetos, estado e comportamento

ESTADO
os valores armazenados nos atributos definem o estado do objeto.

COMPORTAMENTO
Os métodos dos objeto definem seu comportamento.

/////////////////////////////////////////////////////////////////////////////////
// Cap04 - Imutabilidade, representação e formato, coesão

IMUTABILIDADE
É referente aos atributos privados.
Ou seja, os que contém final.


MÉTODO ESTÁTICO EM JAVA:
Os métodos static ou métodos da classe são funções que não dependem de 
nenhuma variável de instância, quando invocados executam uma função sem 
a dependência do conteúdo de um objeto ou a execução da instância de uma 
classe, conseguindo chamar direto qualquer método da classe e também 
manipulando alguns campos da classe.

Os métodos static tem um relacionamento com uma classe como um todo, 
enquanto os métodos que não são static são associados a uma instância de 
classe específica (objeto) e podem manipular as variáveis de instância do 
objeto, como pode ser visto nos exemplos de declarações de métodos.
